#!/usr/bin/env bash
# Pin versions for the HAProxy packaging pipeline.
# Override via environment variables when invoking GoReleaser.

export HAPROXY_VERSION=${HAPROXY_VERSION:-2.8.16}
export PACKAGE_RELEASE=${PACKAGE_RELEASE:-1}
export ROCKY_BASE_IMAGE=${ROCKY_BASE_IMAGE:-rockylinux:9}
